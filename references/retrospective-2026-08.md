# 三周企微文档工作复盘（2026-08-04 ~ 08-24）(索引)

> 原文件 7509 字符超 5000 门禁,已拆为 2 个分片(2026-09-08,只搬不删,备份 `references/retrospective-2026-08.md.bak-20260908`)。
> 新增坑:追加到对应分片,不要往本索引写正文。

## 分片索引

- `references/retrospective-2026-08/p1.md` — 一、同步管线专项（飞书知识库 → 企微 SmartPage v1→v3 演进）;时间线（教训密度最高的三周）;L1 长期文档必须用**用户本人身份**创建，不能用机器人身份;L2 `listAfter` 是 PREPEND：按源树顺序正序创建 = 顺序全反;L3 同一要求被用户重复提出 ≥2 次 = 流程性缺陷;L4 删除文档 ≠ 清空内容，"删了"必须 readback 验证…
- `references/retrospective-2026-08/p2.md` — L10 飞书 Wiki API 的 parent_node_token 有坑：字段名 `_parent_node_token`;L11 多用户身份隔离：MCP 应用 token ≠ 用户权限;L12 全量重建的代价：能增量就别重建;L13 权限变更产生待发布，但不推进版本号 → 验证逻辑假绿;L14 字号变大 = 客户端缩放，先查数据层再下结论;二、行为级教训（跨任务通用）…
